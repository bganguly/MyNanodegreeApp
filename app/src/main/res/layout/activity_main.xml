<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#BBDEFB"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:text="@string/app_title"
        android:textColor="@color/black"
        android:textSize="@dimen/defaultTextSize"
        android:layout_width="match_parent"
        android:layout_height="@dimen/layoutHeight"
        android:layout_marginBottom="@dimen/layoutMarginBottom"
        android:gravity="center"/>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:background="#BBDEFB"
        android:layout_width="@dimen/layoutWidth"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_gravity="center_horizontal"
        tools:context=".MainActivity">

        <Button
            style="@style/ButtonStyles"
            android:text="@string/app_streamer"
            android:id="@+id/app_streamer"/>

        <Button
            style="@style/ButtonStyles"
            android:text="@string/app_football"
            android:id="@+id/app_football"/>

        <Button
            style="@style/ButtonStyles"
            android:text="@string/app_library"
            android:id="@+id/app_library"/>

        <Button
            style="@style/ButtonStyles"
            android:text="@string/app_build_it_bigger"
            android:id="@+id/app_build_it_bigger"/>

        <Button
            style="@style/ButtonStyles"
            android:text="@string/app_reader"
            android:id="@+id/app_reader"/>

        <Button
            style="@style/ButtonStyles.OrangeButtonStyles"
            android:text="@string/app_capstone"
            android:id="@+id/app_capstone"/>

    </LinearLayout>

</LinearLayout>
