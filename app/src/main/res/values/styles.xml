<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonStyles" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:onClick">showMessage</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/lightCoralCustom</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/buttonMargin</item>
        <item name="android:padding">@dimen/buttonPadding</item>
    </style>

    <style name="ButtonStyles.OrangeButtonStyles">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/deepOrangeCustom</item>
    </style>
</resources>
